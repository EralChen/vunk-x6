{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: ssr:dev",
      "request": "launch",
      "command": "pnpm run ssr:dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}